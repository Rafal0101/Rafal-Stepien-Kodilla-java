package com.kodilla.good.patterns.challenges.food2door;

public interface Shop {
//    void process(Provider provider, Product product, Boolean isAvailable);
    void process (ProviderProductDatabase providerProductDatabase);
}
